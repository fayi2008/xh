<?php
$config = array(
    'LOAD_EXT_CONFIG' => array('TTS_CONFIG'=>'tts_config')
);
$route_config = array (
   
);
$all_config = array_merge ( $config, $route_config );

return $all_config;

